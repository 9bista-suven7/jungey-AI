# The live system ships without an initramfs (the installer makes one), and
# update-initramfs wants a file to take over. See Launchpad bug 1829805.
---
dontChroot: true
timeout: 30
script:
    - "touch ${ROOT}/boot/initrd.img-$(uname -r)"
